package com.pouyaheydari.sample.map.android.pojo

/**
 * VehicleTypes that we currently get from the server
 *
 * @property type
 */
enum class VehicleTypeEnum(val type: String) {
    ECO("ECO"),
    PLUS("PLUS")
}